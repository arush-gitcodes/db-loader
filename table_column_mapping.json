{
  "loan": {
    "data_file": "bank_loan",
    "target_columns": ["loan_id", "account_id", "interest_rate"],
    "source_columns": ["loan_id", "account_id", "amount", "interest_rate", "start_date","end_date"],
    "transformation_query": "SELECT loan_id, RIGHT(account_id, 2) AS account_id ,interest_rate FROM <DUCKDB>"
  },
  "employee": {
    "data_file": "employee",
    "target_columns": ["emp_id","emp_name","department_code"],
    "source_columns": ["emp_id","emp_name","department","joining_date"],
    "transformation_query": "SELECT CAST(emp_id AS INTEGER) AS emp_id, emp_name, LEFT(department, 3) AS department_code FROM <DUCKDB>"
  },
  "party": {
    "data_file": "party",
    "target_columns": ["partyid", "firstname", "lastname" , "email"],
    "source_columns": ["partyid", "firstname", "lastname" , "email" , "phone"],
    "transformation_query": ""
  }
}
